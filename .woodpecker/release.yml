pipeline:
  install:
    image: node:20-slim
    commands:
      - npm ci
  release:
    image: plugins/npm
    settings:
      repo: ${CI_REPO}
      tags:
        - "${CI_COMMIT_TAG##v}"
        - "latest"
      secrets: [ docker_username, docker_password ]
when:
  event: tag
  tag: v*